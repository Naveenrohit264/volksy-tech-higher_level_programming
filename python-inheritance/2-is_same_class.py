#!/usr/bin/python3
"""defining function to check whether 
   the type of object is exactly an instance of class"""


def is_same_class(obj, a_class):
    """returns True if type of objects is class
    else return false"""
    if type(obj) is class:
        return True
    else:
        False
