#!/usr/bin/python3
"""function to see if 
   object is exactly an instance of class"""


def is_same_class(obj, a_class):
    """returns True if object is exactly an instance of class"""
    if type(obj) is class:
        return True
    else:
        False
